apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: <name>
  namespace: <namespace>
spec:
  resources:
    requests:
      storage: <size>
  storageClassName: longhorn-crypto
  accessModes:
    - ReadWriteOnce