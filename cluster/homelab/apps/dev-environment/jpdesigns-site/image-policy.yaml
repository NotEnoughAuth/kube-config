apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: jpdesigns-site
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: jpdesigns-site
  policy:
    numerical:
      order: asc
