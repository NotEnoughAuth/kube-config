apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: ws-header
  namespace: headscale
spec:
  headers:
    customRequestHeaders:
      Connection: keep-alive, Upgrade
      Upgrade: WebSocket
