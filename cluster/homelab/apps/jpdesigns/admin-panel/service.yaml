apiVersion: v1
kind: Service
metadata:
  name: jpdesigns-admin
  namespace: jpdesigns
spec:
  type: LoadBalancer
  loadBalancerIP: 10.28.10.50
  selector:
    app: jpdesigns-admin
  ports:
    - port: 3000
      targetPort: 3000
