apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: jpdesigns-admin
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: jpdesigns-admin
  policy:
    numerical:
      order: asc
