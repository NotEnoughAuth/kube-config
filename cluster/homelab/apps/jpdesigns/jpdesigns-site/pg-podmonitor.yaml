apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: jpdesigns-db-monitor
  namespace: jpdesigns
spec:
  selector:
    matchLabels:
      "cnpg.io/cluster": jpdesigns-db
  podMetricsEndpoints:
    - port: metrics
