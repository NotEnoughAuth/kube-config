apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: local-ip-list
  namespace: traefik
  annotations:
    reflector.v1.k8s.emberstack.com/reflection-allowed: "true"
    reflector.v1.k8s.emberstack.com/reflection-allowed-namespaces: "traefik, longhorn-system, dev-environment, external, media-server"
spec:
  ipWhiteList:
    sourceRange:
      - 10.27.20.0/24
      # - 10.27.10.0/24
      - 172.16.16.0/24
