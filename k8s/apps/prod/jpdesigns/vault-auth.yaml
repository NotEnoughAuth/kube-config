apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: jpdesigns-kubernetes-auth
  namespace: jpdesigns
spec:
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: vault-secrets-operator
    serviceAccount: jpdesigns-vault-auth
