apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: ghost-kubernetes-auth
  namespace: blog
spec:
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: vault-secrets-operator
    serviceAccount: ghost-vault-auth
