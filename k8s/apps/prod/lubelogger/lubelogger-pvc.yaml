apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lubelogger-data
  namespace: lubelogger
spec:
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block
  accessModes:
    - ReadWriteMany
