apiVersion: apps/v1
kind: Deployment
metadata:
  name: newt
  namespace: pangolin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: newt
  template:
    metadata:
      labels:
        app: newt
    spec:
      containers:
        - name: newt
          image: fosrl/newt:1.10.4
          envFrom:
            - secretRef:
                name: newt-secret
