apiVersion: v1
kind: Service
metadata:
  name: traefikauth
  namespace: ms-sso
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: containous
    task: traefikauth