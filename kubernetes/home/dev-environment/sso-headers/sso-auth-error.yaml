apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: sso-auth-error
  namespace: dev-environment
spec:
  errors:
    status:
      - "401-403"
    service:
      name: traefikauth
      port: 80
    #   namespace: ms-sso
    query: "auth.opryga.com/oauth2/start?rd={url}"
